name: 🚀 Build & Release IN Studio Files

on:
  push:
    branches: [ "main" ]
    tags:
      - 'v*'
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    name: 🔨 Build Application
    runs-on: windows-latest
    
    strategy:
      matrix:
        configuration: [Debug, Release]
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: 🔧 Setup .NET 8.0
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: 🔨 Setup MSBuild
      uses: microsoft/setup-msbuild@v2

    - name: 📦 Restore dependencies
      run: dotnet restore AutoCopy.sln

    - name: 🏗️ Build solution
      run: dotnet build AutoCopy.sln --configuration ${{ matrix.configuration }} --no-restore

    - name: 📊 Display build info
      run: |
        echo "✅ Build completed successfully!"
        echo "Configuration: ${{ matrix.configuration }}"
        echo "Project: IN Studio Files v1.1"

    - name: 📤 Upload build artifacts (Release only)
      if: matrix.configuration == 'Release'
      uses: actions/upload-artifact@v4
      with:
        name: INStudioFiles-${{ matrix.configuration }}
        path: |
          bin/Release/net8.0-windows/
          !bin/Release/net8.0-windows/*.pdb
        retention-days: 30

  publish:
    name: 📦 Publish Self-Contained
    runs-on: windows-latest
    needs: build
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v'))
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4

    - name: 🔧 Setup .NET 8.0
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: 📦 Publish Windows x64 (Self-Contained)
      run: |
        dotnet publish AutoCopy.csproj `
          -c Release `
          -r win-x64 `
          --self-contained `
          -p:PublishSingleFile=true `
          -p:IncludeNativeLibrariesForSelfExtract=true `
          -p:EnableCompressionInSingleFile=true `
          -p:DebugType=None `
          -p:DebugSymbols=false

    - name: 📊 Display publish info
      run: |
        echo "✅ Publish completed!"
        $size = (Get-Item "bin/Release/net8.0-windows/win-x64/publish/INStudioFiles.exe").Length / 1MB
        echo "📦 Executable size: $([math]::Round($size, 2)) MB"

    - name: 📤 Upload published executable
      uses: actions/upload-artifact@v4
      with:
        name: INStudioFiles-Standalone-x64
        path: bin/Release/net8.0-windows/win-x64/publish/
        retention-days: 90

  release:
    name: 🎉 Create GitHub Release
    runs-on: windows-latest
    needs: publish
    if: startsWith(github.ref, 'refs/tags/v') || github.event_name == 'workflow_dispatch'
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4

    - name: 🔧 Setup .NET 8.0
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: 📦 Publish Release Build
      run: |
        dotnet publish AutoCopy.csproj `
          -c Release `
          -r win-x64 `
          --self-contained `
          -p:PublishSingleFile=true `
          -p:IncludeNativeLibrariesForSelfExtract=true `
          -p:EnableCompressionInSingleFile=true `
          -p:DebugType=None `
          -p:DebugSymbols=false

    - name: 📦 Create ZIP archive
      run: |
        $publishPath = "bin/Release/net8.0-windows/win-x64/publish"
        $version = if ("${{ github.ref_name }}" -like "v*") { "${{ github.ref_name }}" } else { "v1.1.0-build-${{ github.run_number }}" }
        Compress-Archive -Path "$publishPath/*" -DestinationPath "INStudioFiles-$version-win-x64.zip"
        echo "✅ ZIP created: INStudioFiles-$version-win-x64.zip"

    - name: 📊 Generate Release Notes
      id: release_notes
      run: |
        $notes = @"
        # 🎨 IN Studio Files ${{ github.ref_name }}
        
        ## ✨ What's New
        - Modern animated UI with 132 animations
        - Dark/Light theme system (66 colors each)
        - Multi-threaded file copy (1-10 threads)
        - Compact progress dashboard
        - File verification system
        - Resume capability
        - 40+ RAW format support
        
        ## 🔧 Improvements
        - Memory leak fixed (proper resource disposal)
        - Smooth scrolling everywhere
        - Equal-sized buttons
        - Professional UI polish
        
        ## 📦 Installation
        1. Download ``INStudioFiles-${{ github.ref_name }}-win-x64.zip``
        2. Extract to any folder
        3. Run ``INStudioFiles.exe``
        4. No .NET installation required (self-contained)
        
        ## 💻 System Requirements
        - Windows 10/11 (x64)
        - 100 MB disk space
        
        ## 🐛 Bug Reports
        Please report issues on GitHub Issues page.
        "@
        
        $notes | Out-File -FilePath release_notes.md -Encoding utf8
        echo "✅ Release notes generated"

    - name: 🎉 Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        files: |
          INStudioFiles-${{ github.ref_name }}-win-x64.zip
          release_notes.md
        body_path: release_notes.md
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  draft-release:
    name: 📝 Create Draft Release (Main Branch)
    runs-on: windows-latest
    needs: publish
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4

    - name: 🔧 Setup .NET 8.0
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: 📦 Publish Release Build
      run: |
        dotnet publish AutoCopy.csproj `
          -c Release `
          -r win-x64 `
          --self-contained `
          -p:PublishSingleFile=true `
          -p:IncludeNativeLibrariesForSelfExtract=true `
          -p:EnableCompressionInSingleFile=true `
          -p:DebugType=None `
          -p:DebugSymbols=false

    - name: 📦 Create ZIP archive
      run: |
        $publishPath = "bin/Release/net8.0-windows/win-x64/publish"
        $version = "v1.1.0-dev-${{ github.run_number }}"
        Compress-Archive -Path "$publishPath/*" -DestinationPath "INStudioFiles-$version-win-x64.zip"
        echo "✅ ZIP created: INStudioFiles-$version-win-x64.zip"

    - name: 📊 Generate Release Notes
      run: |
        $notes = @"
        # 🎨 IN Studio Files v1.1.0-dev-${{ github.run_number }}
        
        **⚠️ Development Build - For Testing Only**
        
        ## ✨ Latest Features
        - Modern animated UI with 132 animations
        - Dark/Light theme system (66 colors each)
        - Multi-threaded file copy (1-10 threads)
        - Compact progress dashboard
        - File verification system
        - Resume capability
        - 40+ RAW format support
        
        ## 🔧 Recent Improvements
        - Memory leak fixed (proper resource disposal)
        - Smooth scrolling everywhere
        - Equal-sized buttons
        - Professional UI polish
        - GitHub Actions workflow
        
        ## 📦 Installation
        1. Download ``INStudioFiles-v1.1.0-dev-${{ github.run_number }}-win-x64.zip``
        2. Extract to any folder
        3. Run ``INStudioFiles.exe``
        4. No .NET installation required (self-contained)
        
        ## 💻 System Requirements
        - Windows 10/11 (x64)
        - 100 MB disk space
        
        **Note**: This is a development build. For stable releases, use tagged versions.
        "@
        
        $notes | Out-File -FilePath release_notes.md -Encoding utf8

    - name: 📝 Create Draft Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v1.1.0-dev-${{ github.run_number }}
        name: 🚧 IN Studio Files v1.1.0-dev-${{ github.run_number }}
        files: |
          INStudioFiles-v1.1.0-dev-${{ github.run_number }}-win-x64.zip
        body_path: release_notes.md
        draft: true
        prerelease: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  code-quality:
    name: 🔍 Code Quality Check
    runs-on: windows-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4

    - name: 🔧 Setup .NET 8.0
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: 📊 Code Metrics
      run: |
        echo "═══════════════════════════════════════"
        echo "📊 CODE METRICS"
        echo "═══════════════════════════════════════"
        
        $xamlLines = (Get-Content MainWindow.xaml).Count
        $csLines = (Get-Content MainWindow.xaml.cs).Count
        $totalFiles = (Get-ChildItem -Recurse -Include *.cs,*.xaml | Where-Object { $_.FullName -notmatch "\\obj\\|\\bin\\" }).Count
        
        echo "📄 XAML: $xamlLines lines"
        echo "💻 C#: $csLines lines"  
        echo "📁 Total files: $totalFiles"
        echo ""
        echo "✅ Code metrics collected!"

    - name: 🔍 Check for common issues
      run: |
        echo "🔍 Checking for potential issues..."
        
        # Check for TODO comments
        $todos = Select-String -Path "*.cs" -Pattern "TODO|FIXME|HACK" -ErrorAction SilentlyContinue
        if ($todos) {
          echo "⚠️ Found TODO/FIXME comments:"
          $todos | Select-Object -First 5
        } else {
          echo "✅ No TODO/FIXME comments found"
        }
        
        echo ""
        echo "✅ Quality check complete!"
